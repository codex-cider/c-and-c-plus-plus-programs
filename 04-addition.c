#include <stdio.h>

void main()
{

    int a, b, sum;

    a = 20;
    b = 30;

    sum = a + b;

    // printf("SUM is %d", sum);

    printf("%d + %d = %d", a, b, sum);
    printf("\n");
    printf("Sum of %d & %d is %d", a, b, sum);

    // 20 + 30 = 50
}