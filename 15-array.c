#include <stdio.h>

// collection of similar type of data is array

int main()
{

    int a[5]; // array declaration = data_type variable[size];

    a[0] = 10;
    a[1] = 20;
    a[2] = 30;
    a[3] = 40;
    a[4] = 50;

    // Print array third element value

    printf("Third element is %d", a[2]);
}
